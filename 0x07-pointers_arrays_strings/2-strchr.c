#include "main.h"
#include <string.h>
/**
 * _strchr - entry
 * @s: string
 * @c: character
 * Return: strchr if successful otherwise fail
 */
char *_strchr(char *s, char c)
{
	return (strchr(s, c));
}

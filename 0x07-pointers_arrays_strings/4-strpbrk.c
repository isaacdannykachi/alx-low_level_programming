#include "main.h"
#include <string.h>
/**
 * _strpbrk - entry
 * @s: string
 * @accept: string
 *
 * Return: strpbrk on success otherwise fail
 */
char *_strpbrk(char *s, char *accept)
{
	return (strpbrk(s, accept));
}

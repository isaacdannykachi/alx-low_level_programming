#include "main.h"
#include <string.h>
/**
 * _strstr - Entry
 * @haystack: string
 * @needle: string
 *
 * Return: strstr on success otherwise fail
 */
char *_strstr(char *haystack, char *needle)
{
	return (strstr(haystack, needle));
}
